#stops, removes and builds latest docker file 
docker rm -f videoserv
docker build -t video:latest .

# Add volume declarations to the Dockerfile
VOLUME /var/mp4s
VOLUME /var/www

docker run -d -p 1935:1935 -p 80:80 --name videoserv -v $PWD/mp4:/var/mp4s -v $PWD/www:/var/www video:latest
